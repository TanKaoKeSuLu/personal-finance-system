<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tankaokesulu.financeapi.mapper.UserMapper">

    <select id="selectByUsername"
            resultType="com.tankaokesulu.financeapi.entity.User">

        SELECT id,
               username,
               password
        FROM user
        WHERE username = #{username}

    </select>

    <insert id="insert">

        INSERT INTO user
        (
            username,
            password,
            create_time
        )
        VALUES
            (
                #{username},
                #{password},
                NOW()
            )

    </insert>
    <select id="selectById" resultType="com.tankaokesulu.financeapi.entity.User">
        SELECT
            id,
            username,
            password,
            create_time
        FROM user
        WHERE id = #{id}
    </select>
    <update id="updatePassword">

        UPDATE user

        SET password = #{password}

        WHERE id = #{id}

    </update>


</mapper>